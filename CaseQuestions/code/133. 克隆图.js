var cloneGraph = function(node) {
  if (!node) return node
  const map = new Map()

  return dfs(node)
  function dfs(node) {
    const = 
  }
}
console.log(
  cloneGraph([
    [2, 4],
    [1, 3],
    [2, 4],
    [1, 3],
  ])
)
